type Dev = {
    user: string;
    skills: string[];
}

const devs: Dev[] = require('./task.json');
const skill = ""

// devuelvan los mejores candidatos que se adapten a mi busqueda

devs.forEach(dev => {
    
})
